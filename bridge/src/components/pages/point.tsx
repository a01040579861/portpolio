const Point = () => {
  return (
    <section className="h-screen flex items-center justify-around text-[var(--light)] mt-10 px-6">
      <div>Point</div>
    </section>
  );
};

export default Point;
